[tool.poetry]
name = "crawler"
version = "0.1.0"
description = ""
authors = ["cuishunjie <cuishunj@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
scrapy = "^2.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
